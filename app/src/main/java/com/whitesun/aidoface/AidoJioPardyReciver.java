package com.whitesun.aidoface;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.util.Log;

import aido.TextToSpeech.BroadcastTTS;

/**
 * Created by rathore on 26/10/17.
 */

public class AidoJioPardyReciver extends BroadcastReceiver {
    BroadcastTTS _ttsaido1;
    @Override
    public void onReceive(Context context, Intent intent) {
//
//
//            _ttsaido1 = new BroadcastTTS(context);
//            Log.i("AIDOMAINRECEIVER","called");
//            if(intent!=null){
//                if(intent.getAction().equals("com.rathore.aidojiopardy")){
//                    Log.i("aidojiopardy","called");
//                    if(intent.getStringExtra("data")!=null) {
//                        _ttsaido1.speak(intent.getStringExtra("data"));
//                    }
//                }
//            }
        }
    }

